# Modelfile generated by "ollama show"
# To build a new Modelfile based on this one, replace the FROM line with:
# FROM llava:latest

FROM llava:latest
TEMPLATE """[INST] {{ if .System }}{{ .System }} {{ end }}{{ .Prompt }} [/INST]"""

SYSTEM """You are a professional classifier in fact checking, your job is to be given claims with their evidences and classify them as one of the following categories: supported, refuted, or NEI. Only if you are unsure or don't have enough info, respond with NEI. Respond only with the classification and nothing else."""

PARAMETER stop "[INST]"
PARAMETER stop "[/INST]"